const TeacherQuestionBank = () => {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold text-foreground">문제은행(교사용)</h1>
        <p className="text-muted-foreground mt-2">
          관리자 승인된 교사만 편집 가능 (모의)
        </p>
      </div>
    </div>
  );
};

export default TeacherQuestionBank;